general:
  build_dir: test-circleci-crashlytics
machine:
  xcode:
    version: "6.3.1"
  environment:
    XCODE_SCHEMA: test-circleci-crashlytics
    XCODE_WORKSPACE: test-circleci-crashlyrics.xcworkspace
