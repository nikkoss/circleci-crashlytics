general:
  build_dir: test-circleci-crashlytics
machine:
  xcode:
    version: "6.3.1"
  environment:
    XCODE_SCHEME: test-circleci-crashlytics
    XCODE_WORKSPACE: test-circleci-crashlytics/test-circleci-crashlytics.xcworkspace
